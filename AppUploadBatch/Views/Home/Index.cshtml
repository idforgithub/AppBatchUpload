@{
    Layout = null;
    ViewBag.Title = "App Upload Batch";
}

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.css" />

    <title>@ViewBag.Title</title>
</head>

<body>
    <div class="container">
        <div class="panel-heading"></div>

        <div class="form-group">
            <h2>
                <i>
                    @ViewBag.Title
                </i>
            </h2>
        </div>

        <div class="form-group">
            <input type="button" class="btn btn-primary" value="Download Upload Excel Template" onclick="location.href='ImportExport/Export'">
            <!--<span class="glyphicon glyphicon-save"></span>-->
        </div>

        <!-- Disini angular!!!-->
        <!--<div ng-app="myAngular" ng-controller="myController">
            <div class="form-group">
                <select ng-model="browseFile" class="form-">
                    <option ng-repeat="x in files" values="{{x.files}}">
                        {{x.files}}
                    </option>
                </select>
            </div>
        </div>-->

        <div class="form-group">
            Upload Excel Template : <input type="file" value="Import Excel Template" accept="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet, application/vnd.ms-excel">
        </div>

        <div class="form-group">
            <button class="btn btn-primary">
                Submit
            </button>
        </div>

    </div>
</body>
</html>